process {
    withName: SAMTOOLS_UMITAGTRANSFER {
        ext.args   = ''
        ext.rx_tag = 'RX'
        ext.prefix = { "${meta.id}.markdup.sorted" }
        publishDir = [
            enabled: false  // intermediate file; not published to results
        ]
    }
}
